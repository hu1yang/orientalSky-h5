export default function Header(){
  return (
      <div className={'flex items-center w-full px-[var(--layout-padding)] h-[var(--header-height)] bg-[var(--primary-color)] sticky left-0 top-0'}>
        header
      </div>
  )
}
